<h2 class="mb-3">Log in</h2>

<%= form_with url: sessions_path do |f| %>
  <div class="field form-group mb-3">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Email", value: params[:email]  %>
  </div>
  <%# value:"ahmad.lubis@gmail.com" %>

  <div class="field form-group mb-2">
    <%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "Password", value:"olegunnarsolksjaer"  %>
  </div>
  <%# value:"olegunnarsolksjaer" %>

  <%# <% if devise_mapping.rememberable? %>
    <div class="field form-group mb-2">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <%# <% end %>

  <div class="actions">
    <%= f.submit "Log in", class: "btn btn-secondary" %>
  </div>
<% end %>

<%# <%= render "devise/shared/links" %>
